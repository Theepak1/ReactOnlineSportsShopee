import React from 'react';
import { connect } from 'react-redux';

const deletePayment = () => {

    return (
        <div>
            Delete Page 
        </div>
    )
};

export default connect()(deletePayment);